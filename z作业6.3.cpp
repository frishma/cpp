#include<bits/stdc++.h>
int reverse(int number);
bool isPalindrome(int number);
using namespace std;
int main()
{
	long number;
	cin>>number;
	isPalindrome(number);
	return 0; 
}
int reverse(int number)
{
	char str[];
	itoa(number,str,10)
	return str;
}
bool ispalindrome(int number)
{
	int b=c=reverse(number);
	for(int a=0;a<=(b/2);a++)
	{
	char mu;
	char max[]=reverse(number);
	}
	bool;
}
